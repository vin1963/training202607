# 點餐系統後端 — 實作練習（考生卷）

| 項目 | 說明 |
|------|------|
| 主題 | 點餐系統 Backend API（Spring Boot + JPA + MySQL） |
| 形式 | **半開卷填空**（提供提示與骨架碼，請補完缺漏） |
| 方式 | 每步驟給「需求 + 提示 + 骨架」，受測者補完空白 |
| 技術棧 | Spring Boot 3.2 / Java 17 / Maven / Spring Data JPA / MySQL |
| 建議時間 | 120 分鐘（含測試） |

---

## 作答規則

1. 依**章節順序**逐步完成，每章有「驗收點」請自行用 Postman/cURL 驗證。
2. 骨架中以 `① ____ ② ____` 標示「要填寫」的位置；`// TODO 產出` 表示「要自己寫的程式」。
3. 不得修改題目給定的欄位結構與方法簽名（簽名是評分依據）。
4. 完成後請確保能通過**第 11 章驗收標準**全部項目。
5. 加分題為選做，不影響及格，但影響總分。

---

# 1. 系統情境與需求

## 1.1 業務背景

一家餐廳推出**線上點餐系統**。顧客瀏覽菜單 → 選擇項目與數量 → 成立訂單。

- 菜單項目分成不同**分類**（主餐 / 飲料 / 點心）。
- 每個項目可設定**今日是否供應**（is_available）。
- 每個項目有**當日庫存**（stock_quantity）。
- 顧客點餐時，系統要：**扣庫存 + 計算總價 + 建立訂單**，整個流程必須是**一個交易**（全部成功或全部失敗）。
- 員工需要**儀表板**看到：熱賣排行、今日營業額、待處理訂單數。

## 1.2 需求規格表

| 編號 | 功能 | 說明 | 層 |
|------|------|------|-----|
| FR-01-01 | 菜單 CRUD | 新增/查詢/編輯/刪除菜單項目 | Service/Controller |
| FR-01-02 | 菜單篩選 | 依分類、供應狀態查詢 | Repository |
| FR-01-03 | 菜單關鍵字搜尋 | 依名稱模糊搜尋（不分大小寫） | Repository |
| FR-02-01 | 建立訂單 | 一張訂單含多筆明細，**同一交易**扣庫存+算總價 | Service |
| FR-02-02 | 訂單查詢 | 依狀態（PENDING/COMPLETED/CANCELLED）篩選 | Repository |
| FR-03-01 | 儀表板統計 | 熱賣排行、今日營業額、待處理訂單數 | Repository |
| FR-03-02 | 售罄清單 | 列出庫存為 0 的菜單項目 | Repository |

## 1.3 給定 ER 圖（關聯已設計好）

```
menu_items (菜單項目)
   id            BIGINT      PK AUTO_INCREMENT
   name          VARCHAR(80) NOT NULL
   category      VARCHAR(30) NOT NULL
   price         DECIMAL(10,2) NOT NULL
   is_available  TINYINT(1)  NOT NULL DEFAULT 1
   stock_quantity INT        NOT NULL DEFAULT 0

orders (訂單主檔)
   id             BIGINT  PK AUTO_INCREMENT
   customer_name  VARCHAR(50) NOT NULL
   order_time     DATETIME    NOT NULL
   status         VARCHAR(20) NOT NULL DEFAULT 'PENDING'   -- PENDING/COMPLETED/CANCELLED
   total_amount   DECIMAL(10,2) NOT NULL

order_items (訂單明細)
   id            BIGINT     PK AUTO_INCREMENT
   order_id      BIGINT     FK → orders.id
   menu_item_id  BIGINT     FK → menu_items.id
   quantity      INT        NOT NULL
   unit_price    DECIMAL(10,2) NOT NULL   -- 下單當下的價格快照
```

關聯：
- `orders 1 ── N order_items`（一張訂單多筆明細）
- `menu_items 1 ── N order_items`（一個菜色被多次點餐，明細記錄快照價）

> 💡 **設計重點**：為什麼 `order_items` 要存 `unit_price` 快照？因為菜單價格以後會改，但**訂單歷史的價格不該變**。

---

# 2. 資料庫設計（填空）

## 2.1 建立資料庫與表格

```sql
CREATE DATABASE IF NOT EXISTS food_order
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;

USE food_order;

-- 菜單項目
CREATE TABLE IF NOT EXISTS menu_items (
    id             BIGINT AUTO_INCREMENT PRIMARY KEY,
    name           VARCHAR(80)      NOT NULL,
    category       VARCHAR(30)      NOT NULL,
    price          DECIMAL(10,2)    NOT NULL,
    is_available   TINYINT(1)       NOT NULL DEFAULT 1,
    stock_quantity INT              NOT NULL DEFAULT 0
);

-- 訂單主檔
CREATE TABLE IF NOT EXISTS orders (
    id            BIGINT AUTO_INCREMENT PRIMARY KEY,
    customer_name VARCHAR(50)   NOT NULL,
    order_time    DATETIME      NOT NULL,
    status        VARCHAR(20)   NOT NULL DEFAULT 'PENDING',
    total_amount  DECIMAL(10,2) NOT NULL
);

-- 訂單明細
CREATE TABLE IF NOT EXISTS order_items (
    id           BIGINT AUTO_INCREMENT PRIMARY KEY,
    order_id     BIGINT       NOT NULL,
    menu_item_id BIGINT       NOT NULL,
    quantity     INT          NOT NULL,
    unit_price   DECIMAL(10,2) NOT NULL,
    CONSTRAINT fk_oi_order
        FOREIGN KEY (order_id) REFERENCES orders(id)
        ON DELETE CASCADE,
    CONSTRAINT fk_oi_menu
        FOREIGN KEY (menu_item_id) REFERENCES menu_items(id)
        ON DELETE RESTRICT
);
```

## 2.2 種子資料（驗證用）

```sql
INSERT INTO menu_items (name, category, price, is_available, stock_quantity) VALUES
('牛肉麵',   '主餐', 150.00, 1, 20),
('滷肉飯',   '主餐',  65.00, 1, 30),
('珍珠奶茶', '飲料',  55.00, 1, 50),
('美式咖啡', '飲料',  80.00, 1, 40),
('薯條',     '點心',  45.00, 1, 15),
('洋蔥圈',   '點心',  50.00, 0, 0);   -- 售罄 + 停售（加分題會用到）
```

## 2.3 動手驗收

```sql
-- 查「今日有供應」的菜單
SELECT id, name, category, price, stock_quantity
FROM menu_items
WHERE is_available = 1;
```

> ① 依上述 ER 圖寫出「查詢各分類的菜單數量與平均價格」的 SQL（GROUP BY 練習）：
> ```sql
> ② ____________________________________________________________________
> ```

---
# 3. 專案骨架與設定檔（填空）

## 3.1 pom.xml（補齊缺失依賴）

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project ...>
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
    </parent>
    <groupId>com.example</groupId>
    <artifactId>order-api</artifactId>
    <version>1.0.0</version>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>
        <!-- ① 提供 REST API（MVC） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>① ____________________</artifactId>
        </dependency>

        <!-- ② 提供 JPA + Hibernate（資料庫存取） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>② ____________________</artifactId>
        </dependency>

        <!-- ③ MySQL 驅動 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>③ ____________________</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- ④ Bean Validation（@NotBlank 等） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>④ ____________________</artifactId>
        </dependency>
    </dependencies>
</project>
```

## 3.2 application.properties（填空）

```properties
# ===== 伺服器 =====
server.port=8080

# ===== MySQL =====
spring.datasource.url=jdbc:mysql://localhost:3306/food_order?useSSL=false&serverTimezone=Asia/Taipei&characterEncoding=UTF-8
spring.datasource.username=⑤ ________
spring.datasource.password=⑥ ________
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===== JPA =====
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=⑦ ________   # 我們已手動建表，這裡只做驗證
spring.jpa.show-sql=true
```

> ⑦ 應該填哪個值？(A) create-drop (B) update (C) validate (D) none
> 因我們已用 SQL 建表，最安全的是 ______（只驗證不修改）。

## 3.3 啟動類（完整提供）

```java
// src/main/java/com/example/order/OrderApplication.java
package com.example.order;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class OrderApplication {
    public static void main(String[] args) {
        SpringApplication.run(OrderApplication.class, args);
    }
}
```

## 3.4 CORS 設定（填空）

```java
// src/main/java/com/example/order/config/CorsConfig.java
package com.example.order.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.web.cors.CorsConfiguration;
import org.springframework.web.cors.UrlBasedCorsConfigurationSource;
import org.springframework.web.filter.CorsFilter;

import java.util.List;

@Configuration
public class CorsConfig {

    @Bean
    public CorsFilter corsFilter() {
        CorsConfiguration config = new CorsConfiguration();
        config.setAllowedOrigins(List.of(
                "http://localhost:4200",   // Angular
                "http://localhost:3000",   // React
                "http://localhost:5173"    // Vite
        ));
        config.setAllowedMethods(List.of("GET", "POST", "PUT", "DELETE", "OPTIONS"));
        config.setAllowedHeaders(List.of("⑧ ________"));   // 放行所有 headers
        config.setAllowCredentials(⑨ ________);           // true/false

        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
        source.registerCorsConfiguration("⑩ ________", config);  // 只對 /api/** 開 CORS
        return new CorsFilter(source);
    }
}
```

## 3.5 驗收

```bash
mvn spring-boot:run
curl http://localhost:8080/api/health   # 若暫無此端點，確認啟動無錯誤即可
```

---

# 4. Entity（填空）

## 4.1 MenuItem（菜單項目）

```java
// src/main/java/com/example/order/model/MenuItem.java
package com.example.order.model;

import jakarta.persistence.*;

@Entity
@Table(name = "menu_items")
public class MenuItem {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(name = "name", nullable = false, length = 80)
    private String name;

    @Column(name = "category", nullable = false, length = 30)
    private String category;

    @Column(name = "price", nullable = false)
    private ⑪ ________ price;              // 請選 BigDecimal 或 double，並解釋原因

    @Column(name = "is_available")
    private boolean available = true;

    @Column(name = "stock_quantity")
    private int stockQuantity;

    // ── TODO 產出：Getter / Setter（含無參數建構子）──
}
```

> ⚠️ **價格為什麼建議用 BigDecimal？**
> double 在計算總價時可能有浮點誤差（0.1+0.2≠0.3）。金融/訂單金額一律用 BigDecimal。

## 4.2 Order（訂單主檔）

```java
// src/main/java/com/example/order/model/Order.java
package com.example.order.model;

import jakarta.persistence.*;
import java.math.BigDecimal;
import java.time.LocalDateTime;
import java.util.ArrayList;
import java.util.List;

@Entity
@Table(name = "orders")
public class Order {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(name = "customer_name", nullable = false, length = 50)
    private String customerName;

    @Column(name = "order_time", nullable = false)
    private LocalDateTime orderTime;

    @Column(name = "status", nullable = false)
    private String status = "PENDING";

    @Column(name = "total_amount", nullable = false)
    private BigDecimal totalAmount;

    // ── 一對多關聯：一張訂單多筆明細 ──
    @① ________(mappedBy = "order", cascade = CascadeType.ALL, orphanRemoval = true)
    private List<OrderItem> items = new ArrayList<>();

    // ── 方便的方法：加明細 ──
    public void addItem(OrderItem item) {
        items.add(item);
        item.setOrder(this);
    }

    // ── TODO 產出：Getter / Setter ──
}
```

> ① 填空：這個關聯是 `@OneToMany` 還是 `@ManyToMany`？為什麼？
> `mappedBy = "order"` 表示**另一方（OrderItem.order）持有外鍵**。

## 4.3 OrderItem（訂單明細）

```java
// src/main/java/com/example/order/model/OrderItem.java
package com.example.order.model;

import jakarta.persistence.*;
import java.math.BigDecimal;

@Entity
@Table(name = "order_items")
public class OrderItem {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne
    @JoinColumn(name = "order_id")
    private Order order;

    @ManyToOne
    @JoinColumn(name = "menu_item_id")
    private MenuItem menuItem;

    @Column(name = "quantity", nullable = false)
    private int quantity;

    @Column(name = "unit_price", nullable = false)
    private BigDecimal unitPrice;   // 下單當下的價格快照

    public BigDecimal getSubtotal() {
        return unitPrice.multiply(BigDecimal.valueOf(quantity));
    }

    // ── TODO 產出：Getter / Setter ──
}
```

## 4.4 驗收（自行檢查）

- [ ] 三個 Entity 有 `@Entity` + `@Table`
- [ ] `Order.items` 用 `@OneToMany(mappedBy="order", cascade=ALL, orphanRemoval=true)`
- [ ] `OrderItem` 對 `Order`、`MenuItem` 都是 `@ManyToOne`
- [ ] 金額欄位都用 `BigDecimal`

---

# 5. Repository（填空）

```java
// src/main/java/com/example/order/repository/MenuItemRepository.java
package com.example.order.repository;

import com.example.order.model.MenuItem;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;

import java.util.List;

public interface MenuItemRepository extends JpaRepository<MenuItem, Long> {

    // FR-01-02：依分類查詢
    List<MenuItem> findByCategory(String category);

    // FR-01-02：查詢「今日有供應」
    List<MenuItem> findByAvailableTrue();

    // FR-03-02：售罄清單（庫存 = 0 且有供應 → 需補貨）
    List<MenuItem> findByStockQuantityAndAvailableTrue(int stockQuantity);

    // FR-01-03：關鍵字搜尋（名稱模糊、不分大小寫）
    @Query("SELECT m FROM MenuItem m WHERE ② ____________________")
    List<MenuItem> searchByName(@Param("keyword") String keyword);

    // FR-03-01：熱賣排行（依銷售數量排序）
    // JOIN order_items + GROUP BY，回傳「菜名 + 銷售總數」
    @Query("SELECT m.name, SUM(oi.quantity) FROM OrderItem oi " +
           "JOIN oi.menuItem m GROUP BY m.id, m.name ORDER BY SUM(oi.quantity) DESC")
    List<Object[]> findTopSellingItems();

    // FR-03-01：各分類銷售統計（名稱、總量、收益）─ 進階
    @Query("SELECT ③ ____________________")
    List<Object[]> getCategorySalesSummary();
}
```

```java
// src/main/java/com/example/order/repository/OrderRepository.java
package com.example.order.repository;

import com.example.order.model.Order;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;

import java.math.BigDecimal;
import java.time.LocalDateTime;
import java.util.List;

public interface OrderRepository extends JpaRepository<Order, Long> {

    // FR-02-02：依狀態查詢
    List<Order> findByStatus(String status);

    // FR-03-01：今日營業額（status=COMPLETED、時間區間）
    @Query("SELECT COALESCE(SUM(o.totalAmount), 0) FROM Order o " +
           "WHERE o.status = 'COMPLETED' AND o.orderTime BETWEEN :start AND :end")
    BigDecimal sumCompletedAmount(@Param("start") LocalDateTime start,
                                  @Param("end") LocalDateTime end);

    // FR-03-01：待處理訂單數
    long ④ _____________;   // 用 Derived Query（countByStatus）
}
```

> ⚠️ **JPQL 提示**：JOIN 可用關係屬性 `JOIN oi.menuItem`，GROUP BY 要含查詢用到的欄位。
> 聚合後傳回 `Object[]`，第 0 格可能是 String/Long/BigDecimal，Service 再轉型。

---

# 6. 驗收

```bash
# 啟動後用 Postman 測試（端點在第八章建立後再測）
# 目前先用 SQL 驗證 Repository 對應的資料存在
```

---
# 7. DTO 與統一格式（填空）

## 7.1 ApiResponse（統一包裝）

```java
// src/main/java/com/example/order/dto/ApiResponse.java
package com.example.order.dto;

public class ApiResponse<T> {

    private boolean success;
    private String  message;
    private T       data;

    public static <T> ApiResponse<T> ok(T data) {
        ApiResponse<T> res = new ApiResponse<>();
        res.success = true;
        res.message = "success";
        res.data    = data;
        return res;
    }

    public static <T> ApiResponse<T> ok(String message, T data) {
        ApiResponse<T> res = new ApiResponse<>();
        res.success = true;
        res.message = message;
        res.data    = data;
        return res;
    }

    // ── TODO 產出：Getter / Setter ──
}
```

## 7.2 MenuItem 請求 DTO（含驗證）

```java
// src/main/java/com/example/order/dto/MenuItemRequest.java
package com.example.order.dto;

import jakarta.validation.constraints.*;

import java.math.BigDecimal;

public class MenuItemRequest {

    @① ________(message = "名稱不可空白")
    @Size(max = 80)
    private String name;

    @NotBlank(message = "分類不可空白")
    private String category;

    @② ________(value = "0", inclusive = false, message = "價格必須大於 0")
    private BigDecimal price;

    private boolean available = true;

    @Min(value = 0, message = "庫存不可為負")
    private int stockQuantity;

    // ── TODO 產出：Getter / Setter ──
}
```

## 7.3 建立訂單請求 DTO（巢狀明細）

```java
// src/main/java/com/example/order/dto/CreateOrderRequest.java
package com.example.order.dto;

import jakarta.validation.Valid;
import jakarta.validation.constraints.NotBlank;
import jakarta.validation.constraints.NotEmpty;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Min;

import java.util.List;

public class CreateOrderRequest {

    @NotBlank(message = "顧客名稱不可空白")
    private String customerName;

    @Valid
    @NotEmpty(message = "至少要點一項")
    private List<OrderItemRequest> items;

    public static class OrderItemRequest {
        @NotNull(message = "菜品 id 不可為空")
        private Long menuItemId;

        @NotNull(message = "數量不可為空")
        @Min(value = 1, message = "數量至少 1")
        private Integer quantity;

        // ── TODO 產出：Getter / Setter ──
    }

    // ── TODO 產出：Getter / Setter ──
}
```

## 7.4 驗收

- [ ] ApiResponse 有 `ok(T)` 與 `ok(message, T)` 兩種工廠
- [ ] 建立訂單時用 `@Valid` 觸發巢狀驗證（在 Controller/Service 端記得加）

---

# 8. Service 商業邏輯（填空）

## 8.1 MenuItemService（CRUD）

```java
// src/main/java/com/example/order/service/MenuItemService.java
package com.example.order.service;

import com.example.order.dto.MenuItemRequest;
import com.example.order.exception.ResourceNotFoundException;
import com.example.order.model.MenuItem;
import com.example.order.repository.MenuItemRepository;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;

@Service
public class MenuItemService {

    private final MenuItemRepository menuRepo;

    public MenuItemService(MenuItemRepository menuRepo) {
        this.menuRepo = menuRepo;
    }

    @Transactional(readOnly = true)
    public List<MenuItem> findAll() {
        return menuRepo.findAll();
    }

    @Transactional(readOnly = true)
    public List<MenuItem> findByCategory(String category) {
        return menuRepo.findByCategory(category);
    }

    @Transactional(readOnly = true)
    public List<MenuItem> findAvailable() {
        return menuRepo.findByAvailableTrue();
    }

    @Transactional(readOnly = true)
    public List<MenuItem> search(String keyword) {
        return menuRepo.searchByName(keyword);
    }

    @Transactional(readOnly = true)
    public MenuItem findById(Long id) {
        return menuRepo.findById(id)
                .orElseThrow(() -> new ResourceNotFoundException(
                        "Menu item not found: " + id));
    }

    @Transactional
    public MenuItem create(MenuItemRequest req) {
        MenuItem m = new MenuItem();
        m.setName(req.getName());
        m.setCategory(req.getCategory());
        m.setPrice(req.getPrice());
        m.setAvailable(req.isAvailable());
        m.setStockQuantity(req.getStockQuantity());
        return menuRepo.save(m);
    }

    @Transactional
    public MenuItem update(Long id, MenuItemRequest req) {
        MenuItem m = findById(id);
        m.setName(req.getName());
        m.setCategory(req.getCategory());
        m.setPrice(req.getPrice());
        m.setAvailable(req.isAvailable());
        m.setStockQuantity(req.getStockQuantity());
        return menuRepo.save(m);
    }

    @Transactional
    public void delete(Long id) {
        MenuItem m = findById(id);
        menuRepo.delete(m);
    }

    @Transactional(readOnly = true)
    public List<MenuItem> findSoldOut() {
        return menuRepo.findByStockQuantityAndAvailableTrue(0);
    }
}
```

## 8.2 OrderService（核心：點餐 = 一道交易）

```java
// src/main/java/com/example/order/service/OrderService.java
package com.example.order.service;

import com.example.order.dto.ApiResponse;
import com.example.order.dto.CreateOrderRequest;
import com.example.order.exception.ResourceNotFoundException;
import com.example.order.exception.StockNotEnoughException; // 自訂例外（第九章建）
import com.example.order.model.MenuItem;
import com.example.order.model.Order;
import com.example.order.model.OrderItem;
import com.example.order.repository.MenuItemRepository;
import com.example.order.repository.OrderRepository;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.math.BigDecimal;
import java.time.LocalDateTime;
import java.util.List;

@Service
public class OrderService {

    private final OrderRepository   orderRepo;
    private final MenuItemRepository menuRepo;

    public OrderService(OrderRepository orderRepo, MenuItemRepository menuRepo) {
        this.orderRepo  = orderRepo;
        this.menuRepo   = menuRepo;
    }

    @Transactional
    public Order createOrder(CreateOrderRequest req) {
        // 1. 建立訂單主檔
        Order order = new Order();
        order.setCustomerName(req.getCustomerName());
        order.setOrderTime(LocalDateTime.now());
        order.setStatus("PENDING");

        BigDecimal total = BigDecimal.ZERO;

        // 2. 逐筆明細：檢查庫存 → 扣庫存 → 建明細 → 累加總價
        for (CreateOrderRequest.OrderItemRequest itemReq : req.getItems()) {
            MenuItem menu = menuRepo.findById(itemReq.getMenuItemId())
                    .orElseThrow(() -> new ResourceNotFoundException(
                            "Menu item not found: " + itemReq.getMenuItemId()));

            if (menu.isAvailable() == false) {
                throw new StockNotEnoughException("已停售: " + menu.getName());
            }
            if (menu.getStockQuantity() < itemReq.getQuantity()) {
                throw new StockNotEnoughException(
                        "庫存不足: " + menu.getName() + " (剩 " + menu.getStockQuantity() + ")");
            }

            // 扣庫存（交易內，失敗會全部回滾）
            menu.setStockQuantity(menu.getStockQuantity() - itemReq.getQuantity());
            menuRepo.save(menu);

            // 價格快照
            OrderItem oi = new OrderItem();
            oi.setMenuItem(menu);
            oi.setQuantity(itemReq.getQuantity());
            oi.setUnitPrice(menu.getPrice());
            order.addItem(oi);

            total = total.add(oi.getSubtotal());
        }

        order.setTotalAmount(total);
        return orderRepo.save(order);   // cascade ALL → items 一起存
    }

    @Transactional(readOnly = true)
    public Order findById(Long id) {
        return orderRepo.findById(id)
                .orElseThrow(() -> new ResourceNotFoundException("Order not found: " + id));
    }

    @Transactional(readOnly = true)
    public List<Order> findByStatus(String status) {
        return orderRepo.findByStatus(status);
    }

    @Transactional
    public Order updateStatus(Long id, String status) {
        Order o = findById(id);
        o.setStatus(status);
        return orderRepo.save(o);
    }

    // ── 儀表板 -------------------------------
    @Transactional(readOnly = true)
    public Object getDashboard() {
        // TODO 產出：組裝「熱賣排行 + 今日營業額 + 待處理訂單數」
        // 提示：
        //   熱賣排行 → menuRepo.findTopSellingItems() 轉成 List<Map<String,Object>>
        //   今日營業額 → orderRepo.sumCompletedAmount(今日0點, 現在)
        //   待處理訂單數 → orderRepo.countByStatus("PENDING")
        return null;   // 替換成 Map
    }
}
```

> **思考題**：為什麼 `createOrder` 若在中途拋例外，前面的**扣庫存也會還原**？
> 因為 `@Transactional` 保證：方法內任何 RuntimeException 未捕獲時，**整個交易回滾**。

---

# 9. 例外處理（填空）

```java
// src/main/java/com/example/order/exception/ResourceNotFoundException.java
package com.example.order.exception;

public class ResourceNotFoundException extends RuntimeException {
    public ResourceNotFoundException(String message) { super(message); }
}
```

```java
// src/main/java/com/example/order/exception/StockNotEnoughException.java
package com.example.order.exception;

// TODO 產出：仿照 ResourceNotFoundException，做一個 RuntimeException
```

```java
// src/main/java/com/example/order/exception/GlobalExceptionHandler.java
package com.example.order.exception;

import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.validation.FieldError;
import org.springframework.web.bind.MethodArgumentNotValidException;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

import java.util.HashMap;
import java.util.Map;

@RestControllerAdvice
public class GlobalExceptionHandler {

    // 資源不存在 → 404
    @ExceptionHandler(ResourceNotFoundException.class)
    public ResponseEntity<Map<String, Object>> handleNotFound(ResourceNotFoundException ex) {
        return buildError(HttpStatus.NOT_FOUND, "NOT_FOUND", ex.getMessage());
    }

    // 庫存不足 → 400
    @ExceptionHandler(StockNotEnoughException.class)
    public ResponseEntity<Map<String, Object>> handleStock(StockNotEnoughException ex) {
        return buildError(HttpStatus.BAD_REQUEST, "STOCK_NOT_ENOUGH", ex.getMessage());
    }

    // 業務規則違反 → 400
    @ExceptionHandler(IllegalArgumentException.class)
    public ResponseEntity<Map<String, Object>> handleBadRequest(IllegalArgumentException ex) {
        return buildError(HttpStatus.BAD_REQUEST, "BAD_REQUEST", ex.getMessage());
    }

    // Bean Validation → 400（多欄位錯誤）
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public ResponseEntity<Map<String, Object>> handleValidation(MethodArgumentNotValidException ex) {
        Map<String, String> fieldErrors = new HashMap<>();
        for (FieldError fe : ex.getBindingResult().getFieldErrors()) {
            fieldErrors.put(fe.getField(), fe.getDefaultMessage());
        }
        Map<String, Object> body = new HashMap<>();
        body.put("success", false);
        body.put("errorCode", "VALIDATION_ERROR");
        body.put("errors", fieldErrors);
        return ResponseEntity.badRequest().body(body);
    }

    private ResponseEntity<Map<String, Object>> buildError(
            HttpStatus status, String errorCode, String message) {
        Map<String, Object> body = new HashMap<>();
        body.put("success", false);
        body.put("errorCode", errorCode);
        body.put("message", message);
        return ResponseEntity.status(status).body(body);
    }
}
```
# 10. Controller REST API（填空）

## 10.1 MenuItemController

```java
// src/main/java/com/example/order/controller/MenuItemController.java
package com.example.order.controller;

import com.example.order.dto.ApiResponse;
import com.example.order.dto.MenuItemRequest;
import com.example.order.model.MenuItem;
import com.example.order.service.MenuItemService;
import jakarta.validation.Valid;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/menu")
public class MenuItemController {

    private final MenuItemService menuService;

    public MenuItemController(MenuItemService menuService) {
        this.menuService = menuService;
    }

    // GET /api/menu?category=&keyword=&available=
    @GetMapping
    public ResponseEntity<ApiResponse<Object>> getAll(
            @RequestParam(required = false) String category,
            @RequestParam(required = false) String keyword,
            @RequestParam(required = false) Boolean available) {

        Object result;
        if (category != null && !category.isBlank()) {
            result = menuService.findByCategory(category);
        } else if (keyword != null && !keyword.isBlank()) {
            result = menuService.search(keyword);
        } else if (available != null && available) {
            result = menuService.findAvailable();
        } else {
            result = menuService.findAll();
        }
        return ResponseEntity.ok(ApiResponse.ok(result));
    }

    @GetMapping("/{id}")
    public ResponseEntity<ApiResponse<Object>> getById(@PathVariable Long id) {
        return ResponseEntity.ok(ApiResponse.ok(menuService.findById(id)));
    }

    // GET /api/menu/sold-out → 售罄清單
    @GetMapping("/① ________")
    public ResponseEntity<ApiResponse<Object>> getSoldOut() {
        return ResponseEntity.ok(ApiResponse.ok(menuService.findSoldOut()));
    }

    @PostMapping
    public ResponseEntity<ApiResponse<Object>> create(@Valid @RequestBody MenuItemRequest req) {
        return ResponseEntity.status(HttpStatus.CREATED)
                .body(ApiResponse.ok("Menu item created", menuService.create(req)));
    }

    @PutMapping("/{id}")
    public ResponseEntity<ApiResponse<Object>> update(
            @PathVariable Long id, @Valid @RequestBody MenuItemRequest req) {
        return ResponseEntity.ok(ApiResponse.ok("Menu item updated",
                menuService.update(id, req)));
    }

    @DeleteMapping("/{id}")
    public ResponseEntity<ApiResponse<Object>> delete(@PathVariable Long id) {
        menuService.delete(id);
        return ResponseEntity.ok(ApiResponse.ok("Menu item deleted", null));
    }
}
```

> ⚠️ **路徑順序陷阱**：`/sold-out` 這類固定路徑**要寫在 `/{id}` 之前**，否則 Spring 會把 "sold-out" 當成 id 傳入。

## 10.2 OrderController

```java
// src/main/java/com/example/order/controller/OrderController.java
package com.example.order.controller;

import com.example.order.dto.ApiResponse;
import com.example.order.dto.CreateOrderRequest;
import com.example.order.model.Order;
import com.example.order.service.OrderService;
import jakarta.validation.Valid;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/orders")
public class OrderController {

    private final OrderService orderService;

    public OrderController(OrderService orderService) {
        this.orderService = orderService;
    }

    // POST /api/orders → 建立訂單
    @PostMapping
    public ResponseEntity<ApiResponse<Object>> create(@Valid @RequestBody CreateOrderRequest req) {
        return ResponseEntity.status(org.springframework.http.HttpStatus.CREATED)
                .body(ApiResponse.ok("Order created", orderService.createOrder(req)));
    }

    // GET /api/orders/{id}
    @GetMapping("/{id}")
    public ResponseEntity<ApiResponse<Object>> getById(@PathVariable Long id) {
        return ResponseEntity.ok(ApiResponse.ok(orderService.findById(id)));
    }

    // GET /api/orders?status=PENDING
    @GetMapping
    public ResponseEntity<ApiResponse<Object>> getByStatus(
            @RequestParam(required = false) String status) {
        List<Order> data = (status == null || status.isBlank())
                ? orderService.findAll()
                : orderService.findByStatus(status);
        return ResponseEntity.ok(ApiResponse.ok(data));
    }

    // PUT /api/orders/{id}/status
    // Body: { "status": "COMPLETED" }
    @PutMapping("/{id}/status")
    public ResponseEntity<ApiResponse<Object>> updateStatus(
            @PathVariable Long id,
            @RequestBody java.util.Map<String, String> body) {
        return ResponseEntity.ok(ApiResponse.ok("Status updated",
                orderService.updateStatus(id, body.get("status"))));
    }

    // GET /api/orders/dashboard
    @GetMapping("/② ________")
    public ResponseEntity<ApiResponse<Object>> getDashboard() {
        return ResponseEntity.ok(ApiResponse.ok(orderService.getDashboard()));
    }
}
```

> ⚠️ `orderService.findAll()` 在 OrderService 需補一個方法（JpaRepository 內建 `findAll()`）。請補加 readOnly 包裝。

---

# 11. 加分題（選做）

## 11.1 送餐號碼牌（難度：★★）

在 `Order` 增加 `queueNo`（排隊序號）欄位：當天第 N 張訂單。

**提示**：`@Column(unique=true)` 不夠，最簡單：在單筆查詢時回傳 `order.getId()` 即為號碼；若要獨立欄位，可 `@PrePersist` 時由當日訂單數+1 產生（注意併發）。

## 11.2 前端呼叫（難度：★★）

用任一前端（React/Vite/Angular）做最簡頁面：列出 `/api/menu` 並套上 CORS。

```tsx
// React 最簡版
import { useEffect, useState } from 'react';
import axios from 'axios';

function Menu() {
  const [items, setItems] = useState([]);
  useEffect(() => {
    axios.get('http://localhost:8080/api/menu').then(r => setItems(r.data.data));
  }, []);
  return (
    <ul>
      {items.map((m: any) => (
        <li key={m.id}>{m.name} — ${m.price}</li>
      ))}
    </ul>
  );
}
export default Menu;
```

## 11.3 今日總訂單數（難度：★★★）

新增 Dashboard 欄位「今日訂單數」：`countByOrderTimeBetween(start, end)`。

---

# 12. 驗收標準（作答完畢自行檢查）

啟動 `mvn spring-boot:run` 後，逐一用 cURL 驗證：

```bash
# ── FR-01 菜單 ──────────────────────────────
# 列出全部
curl http://localhost:8080/api/menu

# 依分類篩選
curl "http://localhost:8080/api/menu?category=主餐"

# 關鍵字搜尋（應找到「牛肉」相關）
curl "http://localhost:8080/api/menu?keyword=牛"

# 只看有供應
curl "http://localhost:8080/api/menu?available=true"

# 售罄清單（期望：find stock=0 且 available → 目前種子無吻合，可將洋蔥圈改回 available）
curl http://localhost:8080/api/menu/sold-out

# 新增菜單
curl -X POST http://localhost:8080/api/menu \
     -H "Content-Type: application/json" \
     -d '{"name":"雞排飯","category":"主餐","price":120,"available":true,"stockQuantity":25}'

# ── FR-02 訂單 ──────────────────────────────
# 建立訂單（牛肉麵 x2 + 珍珠奶茶 x1）
curl -X POST http://localhost:8080/api/orders \
     -H "Content-Type: application/json" \
     -d '{"customerName":"小明","items":[{"menuItemId":1,"quantity":2},{"menuItemId":3,"quantity":1}]}'

# 驗證訂單總額（150*2 + 55*1 = 355）
curl http://localhost:8080/api/orders/1

# 查 PENDING
curl "http://localhost:8080/api/orders?status=PENDING"

# 更新狀態
curl -X PUT http://localhost:8080/api/orders/1/status \
     -H "Content-Type: application/json" \
     -d '{"status":"COMPLETED"}'

# ── FR-03 儀表板 ────────────────────────────
curl http://localhost:8080/api/orders/dashboard

# ── 錯誤情境 ────────────────────────────────
# 404：找不到菜品
curl -i http://localhost:8080/api/menu/999

# 400：庫存不足（牛肉麵剩 18 後要 50 份）
curl -X POST http://localhost:8080/api/orders \
     -H "Content-Type: application/json" \
     -d '{"customerName":"庫存測","items":[{"menuItemId":1,"quantity":50}]}'
# 期望：errorCode = STOCK_NOT_ENOUGH

# 400：驗證失敗（價格 0）
curl -X POST http://localhost:8080/api/menu \
     -H "Content-Type: application/json" \
     -d '{"name":"","category":"主餐","price":0,"available":true,"stockQuantity":1}'
# 期望：errorCode = VALIDATION_ERROR 且列出欄位錯誤
```

## 驗收清單

- [ ] 所有 FR-01 / FR-02 / FR-03 端點回傳 `{ success, message, data }`
- [ ] 建立訂單一次扣庫存 2+1 = 3，訂單總額正確（355.00）
- [ ] 庫存不足時回 400 + `STOCK_NOT_ENOUGH` 且**庫存不變**
- [ ] 找不到資源回 404 + `NOT_FOUND`
- [ ] 驗證失敗回 400 + `VALIDATION_ERROR` 含 `errors` 物件
- [ ] Dashboard 回傳熱賣排行 / 今日營業額 / 待處理訂單數（加分：今日訂單數）
- [ ] CORS：前端 4200/3000/5173 可存取 `/api/**`

---

# 附錄：配分參考（考官用）

> 實際配分明細見《點餐系統後端-考官解答與評分.md》

| 章節 | 配分 |
|------|------|
| 2 資料庫 | 10 |
| 3 骨架+設定+CORS | 15 |
| 4 Entity | 15 |
| 5 Repository | 20 |
| 7 DTO | 10 |
| 8 Service | 15 |
| 9 例外 | 5 |
| 10 Controller | 10 |
| 加分題 | +10 |
| **合計** | **100 + 10** |

> 及格門檻：60 分（扣掉加分題）。

---

## 作答完成提醒

- 整理成一個可編譯、可啟動的 Spring Boot 專案。
- 提供：所有 Java 檔 + 建表 SQL + 一次能跑通的啟動方式。
- 上傳前先自行跑完第 12 章全部 cURL，確認綠燈。

**祝考試順利！**
